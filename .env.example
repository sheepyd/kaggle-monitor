# ===================================
# Kaggle Competition Monitor 配置文件
# ===================================
# 请将此文件重命名为 .env 并填入你的配置信息
# 注意：.env 文件包含敏感信息，请勿提交到版本控制系统

# -----------------------------------
# 1. Kaggle API 认证 (必填)
# -----------------------------------
# 获取方式: Kaggle -> Settings -> API -> Create New Token
# 下载 kaggle.json 后，将其中的值填入此处
KAGGLE_USERNAME=your_kaggle_username
KAGGLE_KEY=your_kaggle_api_key

# -----------------------------------
# 2. 监控设置
# -----------------------------------
# 关键词列表 (英文逗号分隔，不区分大小写)
# 程序会扫描比赛的标题和描述，匹配任意一个关键词即会推送
# 示例关键词:
# - 点云/3D: 3d,point cloud,lidar,depth estimation
# - CV: segmentation,detection,classification,transformer
# - NLP: nlp,llm,text,language
# - 表格: tabular,financial,time series
KEYWORDS=3d,point cloud,lidar,segmentation,transformer,depth,nlp

# 检查频率 (单位: 小时)
# 建议设置为 2-6 小时，过于频繁可能触发 API 限制
CHECK_INTERVAL_HOURS=4

# -----------------------------------
# 3. 邮件通知设置 (SMTP)
# -----------------------------------
# 发件人邮箱地址
SENDER_EMAIL=your_email@qq.com

# 发件人邮箱授权码 (注意: 不是邮箱登录密码!)
# QQ邮箱: 设置 -> 账户 -> POP3/SMTP服务 -> 开启并获取授权码
# Gmail: Google账户 -> 安全 -> 两步验证 -> 应用专用密码
# 163邮箱: 设置 -> POP3/SMTP/IMAP -> 开启并设置授权码
SENDER_PASSWORD=your_email_authorization_code

# 收件人邮箱地址 (接收通知的邮箱)
RECEIVER_EMAIL=receiver@example.com

# SMTP 服务器设置
# QQ邮箱:   smtp.qq.com, 端口 465 (SSL)
# Gmail:    smtp.gmail.com, 端口 465 (SSL) 或 587 (TLS)
# 163邮箱:  smtp.163.com, 端口 465 (SSL)
# Outlook:  smtp.office365.com, 端口 587 (TLS)
SMTP_SERVER=smtp.qq.com
SMTP_PORT=465

# -----------------------------------
# 4. 代理设置 (可选)
# -----------------------------------
# 如果你的服务器无法直接访问 Kaggle，可以配置代理
# 取消注释以下行并填入代理地址
# https_proxy=http://127.0.0.1:7890
# http_proxy=http://127.0.0.1:7890
